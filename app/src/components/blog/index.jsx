import React from 'react';

export class Blog extends React.Component {
  
  constructor (props) {
    super(props);
  }

  render () {
    // TODO: encapsulate all blog posts dynamically here for display and "carousel" access
    return <div style={{backgroundColor: 'blue', width: '100%', height: '100%'}}>React App</div>;
  }
}