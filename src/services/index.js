class BlogAPI {

  constructor () {
    // interface with a GraphQL api layer to retrieve stored blog posts
    
    // ideas:
      // - Admin Portal: Editor => Publish content (data) to GraphQL layer => Fetch data => Dynamically generate HTML (Editor "look") to send to client
  }
}